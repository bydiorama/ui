{
  "name": "@bydiorama/ui",
  "version": "0.0.0",
  "description": "The consumer CLI for the bydiorama/ui registry: `add` installs items and their registry dependencies directly (the owned channel — no third-party CLI in the path) and locks them as it goes; `lock`/`sync` compute drift between an installed item and its current upstream, distinguishing a consumer's own edits from an unapplied upstream change, and report which ledger entries apply. Not yet published — see README.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "bin": {
    "bydiorama-ui": "./bin/ui.ts"
  },
  "scripts": {
    "lock": "node --experimental-strip-types bin/ui.ts lock",
    "sync": "node --experimental-strip-types bin/ui.ts sync"
  },
  "files": [
    "src",
    "bin"
  ]
}
