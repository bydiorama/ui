{
  "name": "@bydiorama/ui",
  "version": "0.0.0",
  "description": "The sync CLI for apps consuming the bydiorama/ui registry (plan.md §4.6). Computes drift between an installed item and its current upstream, distinguishing a consumer's own edits from an unapplied upstream change, and reports which ledger entries apply. Not yet published — see README.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "bin": {
    "bydiorama-ui": "./bin/ui.ts"
  },
  "scripts": {
    "lock": "node --experimental-strip-types bin/ui.ts lock",
    "sync": "node --experimental-strip-types bin/ui.ts sync"
  },
  "files": ["src", "bin"]
}
